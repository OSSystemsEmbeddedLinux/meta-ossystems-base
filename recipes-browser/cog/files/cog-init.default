# Use this file to add all Cog related options, e.g. set
# COG_PLATFORM_FDO_VIEW_FULLSCREEN or other variable.
#
# Change the COG_ARGS to your own use, defining the correct platform
# (-P option), URL to open or other argument.

XDG_RUNTIME_DIR=/run/user/0
COG_ARGS="-P fdo https://github.com/Igalia/cog"
